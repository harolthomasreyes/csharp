<Window x:Class="MyPdfEditor.MyPdfEditor.WPF.Views.Dialogs.NewDocumentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPdfEditor.MyPdfEditor.WPF.Views.Dialogs"
        mc:Ignorable="d"
        Title="NewDocumentDialog" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="MinWidth" Value="80" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="RadioButton">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Document Name -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Document Name:" />
        <TextBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" 
                 Text="{Binding DocumentName, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Page Size -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Page Size:" />
        <ComboBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2"
                  ItemsSource="{Binding PageSizes}"
                  SelectedItem="{Binding SelectedPageSize}"
                  DisplayMemberPath="Name" />

        <!-- Orientation -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Orientation:" />
        <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal">
            <RadioButton Content="Portrait" IsChecked="{Binding IsPortrait}" GroupName="Orientation" />
            <RadioButton Content="Landscape" IsChecked="{Binding IsLandscape}" GroupName="Orientation" />
        </StackPanel>

        <!-- Custom Size -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Width:" 
                   Visibility="{Binding IsCustomSize, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBox Grid.Row="3" Grid.Column="1" 
                 Text="{Binding CustomWidth, UpdateSourceTrigger=PropertyChanged}"
                 Visibility="{Binding IsCustomSize, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBlock Grid.Row="3" Grid.Column="2" Text="mm"
                   Visibility="{Binding IsCustomSize, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Height:" 
                   Visibility="{Binding IsCustomSize, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBox Grid.Row="4" Grid.Column="1" 
                 Text="{Binding CustomHeight, UpdateSourceTrigger=PropertyChanged}"
                 Visibility="{Binding IsCustomSize, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <TextBlock Grid.Row="4" Grid.Column="2" Text="mm"
                   Visibility="{Binding IsCustomSize, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <!-- Margins -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Margins:" />
        <ComboBox Grid.Row="5" Grid.Column="1" Grid.ColumnSpan="2"
                  ItemsSource="{Binding MarginPresets}"
                  SelectedItem="{Binding SelectedMargin}"
                  DisplayMemberPath="Name" />

        <!-- Number of Pages -->
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Number of Pages:" />
        <TextBox Grid.Row="6" Grid.Column="1" Grid.ColumnSpan="2"
                 Text="{Binding PageCount, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Spacer -->
        <Rectangle Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="3" Fill="Transparent" />

        <!-- Buttons -->
        <StackPanel Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="3" 
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OKButton_Click" 
                    IsEnabled="{Binding IsValid}" />
            <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
