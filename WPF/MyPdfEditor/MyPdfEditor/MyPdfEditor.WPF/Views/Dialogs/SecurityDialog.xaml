<Window x:Class="MyPdfEditor.MyPdfEditor.WPF.Views.Dialogs.SecurityDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPdfEditor.MyPdfEditor.WPF.Views.Dialogs"
        mc:Ignorable="d"
        Title="SecurityDialog" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10" />
            <Setter Property="Padding" Value="10" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="MinWidth" Value="80" />
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Encryption Settings -->
        <GroupBox Grid.Row="0" Header="Encryption Settings">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Encryption Type:" />
                <ComboBox Grid.Row="0" Grid.Column="1" 
                          ItemsSource="{Binding EncryptionTypes}"
                          SelectedItem="{Binding SelectedEncryptionType}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Key Length:" />
                <ComboBox Grid.Row="1" Grid.Column="1" 
                          ItemsSource="{Binding KeyLengths}"
                          SelectedItem="{Binding SelectedKeyLength}"
                          IsEnabled="{Binding IsAesSelected}" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Enable Encryption" />
                <CheckBox Grid.Row="2" Grid.Column="1" 
                          IsChecked="{Binding IsEncryptionEnabled}" />
            </Grid>
        </GroupBox>

        <!-- Passwords -->
        <GroupBox Grid.Row="1" Header="Passwords" 
                  Visibility="{Binding IsEncryptionEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="User Password:" />
                <PasswordBox Grid.Row="0" Grid.Column="1" 
                             Password="{Binding UserPassword, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="0" Grid.Column="2" Content="Generate" 
                        Click="GeneratePassword_Click" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Owner Password:" />
                <PasswordBox Grid.Row="1" Grid.Column="1" 
                             Password="{Binding OwnerPassword, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="1" Grid.Column="2" Content="Generate" 
                        Click="GeneratePassword_Click" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Confirm Password:" />
                <PasswordBox Grid.Row="2" Grid.Column="1" 
                             Password="{Binding ConfirmPassword, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </GroupBox>

        <!-- Permissions -->
        <GroupBox Grid.Row="2" Header="Permissions"
                  Visibility="{Binding IsEncryptionEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <CheckBox Grid.Row="0" Grid.Column="0" Content="Allow Printing"
                          IsChecked="{Binding AllowPrinting}" />
                <CheckBox Grid.Row="0" Grid.Column="1" Content="Allow High Quality Printing"
                          IsChecked="{Binding AllowHighQualityPrinting}" />

                <CheckBox Grid.Row="1" Grid.Column="0" Content="Allow Modifications"
                          IsChecked="{Binding AllowModifications}" />
                <CheckBox Grid.Row="1" Grid.Column="1" Content="Allow Copy Content"
                          IsChecked="{Binding AllowCopyContent}" />

                <CheckBox Grid.Row="2" Grid.Column="0" Content="Allow Annotations"
                          IsChecked="{Binding AllowAnnotations}" />
                <CheckBox Grid.Row="2" Grid.Column="1" Content="Allow Form Filling"
                          IsChecked="{Binding AllowFormFilling}" />

                <CheckBox Grid.Row="3" Grid.Column="0" Content="Allow Accessibility"
                          IsChecked="{Binding AllowAccessibility}" />
                <CheckBox Grid.Row="3" Grid.Column="1" Content="Allow Document Assembly"
                          IsChecked="{Binding AllowDocumentAssembly}" />
            </Grid>
        </GroupBox>

        <!-- Password Strength -->
        <GroupBox Grid.Row="3" Header="Password Strength"
                  Visibility="{Binding IsEncryptionEnabled, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <ProgressBar Grid.Row="0" Height="10" 
                             Value="{Binding PasswordStrength}" 
                             Maximum="100" />

                <TextBlock Grid.Row="1" Text="{Binding PasswordStrengthDescription}"
                           HorizontalAlignment="Center" />
            </Grid>
        </GroupBox>
        
        <!-- Spacer -->
        <Rectangle Grid.Row="4" Fill="Transparent" />

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="OKButton_Click" 
                IsEnabled="{Binding IsValid}" />
            <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
