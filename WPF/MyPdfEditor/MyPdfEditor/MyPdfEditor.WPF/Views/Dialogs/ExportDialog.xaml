<Window x:Class="MyPdfEditor.MyPdfEditor.WPF.Views.Dialogs.ExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyPdfEditor.MyPdfEditor.WPF.Views.Dialogs"
        mc:Ignorable="d"
        Title="ExportDialog" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="10" />
            <Setter Property="Padding" Value="10" />
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="MinWidth" Value="80" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Format Selection -->
        <GroupBox Grid.Row="0" Header="Export Format">
            <ComboBox ItemsSource="{Binding ExportFormats}" 
                      SelectedItem="{Binding SelectedFormat}"
                      DisplayMemberPath="Description" />
        </GroupBox>

        <!-- File Location -->
        <GroupBox Grid.Row="1" Header="File Location">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0" Text="{Binding FilePath}" />
                <Button Grid.Column="1" Content="Browse..." 
                        Click="BrowseButton_Click" />
            </Grid>
        </GroupBox>

        <!-- Format Specific Options -->
        <GroupBox Grid.Row="2" Header="Options"
                  Visibility="{Binding IsImageFormat, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="DPI:" />
                <ComboBox Grid.Row="0" Grid.Column="1" 
                          ItemsSource="{Binding DpiOptions}"
                          SelectedItem="{Binding SelectedDpi}" />

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Image Format:" />
                <ComboBox Grid.Row="1" Grid.Column="1" 
                          ItemsSource="{Binding ImageFormats}"
                          SelectedItem="{Binding SelectedImageFormat}" />
            </Grid>
        </GroupBox>

        <!-- Page Range -->
        <GroupBox Grid.Row="3" Header="Page Range">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                          ItemsSource="{Binding PageRangeOptions}"
                          SelectedItem="{Binding SelectedPageRange}" />

                <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal"
                           Visibility="{Binding IsCustomRange, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBox Width="50" Text="{Binding StartPage}" />
                    <TextBlock Text="to" VerticalAlignment="Center" Margin="5,0" />
                    <TextBox Width="50" Text="{Binding EndPage}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Additional Options -->
        <GroupBox Grid.Row="4" Header="Additional Options">
            <StackPanel>
                <CheckBox Content="Include Form Fields" 
                          IsChecked="{Binding IncludeFormFields}" />
                <CheckBox Content="Include Annotations" 
                          IsChecked="{Binding IncludeAnnotations}" />
                <CheckBox Content="Optimize for Web" 
                          IsChecked="{Binding OptimizeForWeb}" />
                <CheckBox Content="Preserve Transparency" 
                          IsChecked="{Binding PreserveTransparency}"
                          Visibility="{Binding IsImageFormat, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </StackPanel>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Export" IsDefault="True" Click="OKButton_Click" 
                    IsEnabled="{Binding IsValid}" />
            <Button Content="Cancel" IsCancel="True" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
